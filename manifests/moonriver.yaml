manifestVersion: subsquid.io/v0.1
name: belopash-moonriver-test
version: 1
description: |-
  blablabla
build:
deploy:
  addons:
    postgres:
  processor:
    env:
      CHAIN: moonriver
    cmd: ["node", "lib/processor"]
  api:
    cmd:
      [
        "npx",
        "squid-graphql-server",
        "--dumb-cache",
        "in-memory",
        "--dumb-cache-ttl",
        "1000",
        "--dumb-cache-size",
        "100",
        "--dumb-cache-max-age",
        "1000",
      ]
scale:
  addons:
    postgres:
      storage: 100Gi
  dedicated: true
